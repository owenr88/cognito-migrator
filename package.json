{
  "name": "cognito-migrator",
  "description": "Migrate users from one Cognito user pool to another",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start --w packages/*",
    "build": "npm run build --workspaces",
    "publish-all": "npm publish --workspaces",
    "test": "npm run test --w packages/*"
  },
  "workspaces": [
    "./packages/zod-cognito",
    "./packages/cognito-export",
    "./packages/cognito-import"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
